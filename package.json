{
  "name": "app-bizu-exercito-brasileiro",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:admin": "npm --workspace @bizu/admin run dev --",
    "dev:mobile": "npm --workspace @bizu/mobile run dev --",
    "lint": "npm run lint --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present"
  }
}
